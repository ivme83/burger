<div class="container">
  <div class="menu-grid">
    <div class="menu-top">
      <h1>Eat Da Burger!</h1>
      <img class="divider-top" src="../assets/img/swirls9.png">
    </div>
    <div class="menu-left">
      <h1>Burgers that have been eaten!</h1>

      <ul>
        {{#each burgers}}
          {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="menu-right">
      <h1>Burgers that have not been eaten!</h1>

      <ul>
        {{#each burgers}}
          {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
          {{/if}}
        {{/each}}
      </ul>
    </div>

    <div class="menu-btm">
      <img class="divider-btm" src="../assets/img/swirls9.png">
      <h1>Add a Burger</h1>
      <br>
      <form class="create-form">

        <div class="form-group">
          <div class="input-holder">
          <label for="burg">Burger Name: </label>
          <input type="text" id="burg" name="name"><img class="txtFrm" src="assets/img/frm.png"></div>
        </div>

        <div class="form-group">
          <label for="devoured">Eaten?</label><br>
          <input type="radio" name="devoured" value="1" checked> Devoured!<br>
          <input type="radio" name="devoured" value="0"> Looking delicious
        </div>

        <button class="myBtn" type="submit"><img class="imgBtn" src="assets/img/rbn-btn.png"><span>Add</span></button>
      </form>
      
    </div>
  </div>
</div>